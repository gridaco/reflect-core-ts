{
  "name": "@reflect-ui/font-utils",
  "version": "0.0.10",
  "main": "dist/index.js",
  "license": "MIT",
  "types": "dist/index.d.ts",
  "scripts": {
    "clean": "rimraf dist",
    "test": "jest",
    "build": "tsc",
    "prepack": "yarn test && yarn clean && yarn build"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "private": false,
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "@types/jest": "^28.1.6",
    "jest": "^28.1.3",
    "ts-jest": "^28.0.7"
  }
}
